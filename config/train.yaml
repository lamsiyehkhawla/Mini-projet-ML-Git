data:
  kind: "iris"
  path: "data/dataset.csv"
  target: "target"
  split:
    test_size: 0.2
    random_state: 42

model:
  name: "logistic_regression"
  max_iter: 2000
  artifacts_dir: "artifacts"
